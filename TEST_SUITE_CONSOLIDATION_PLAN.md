# План консолидации тестового набора backend

Статус: составлен 2026-09-06 после закрытия
[BACKEND_LAYERED_ARCHITECTURE_REFACTOR_PLAN.md](./BACKEND_LAYERED_ARCHITECTURE_REFACTOR_PLAN.md).

Цель: уменьшить набор в 4–5 раз, не потеряв ни одной проверяемой гарантии.
Тестов должно стать меньше, а сообщений об ошибке — точнее.

## Журнал выполнения

| Этап | Статус | Тестов после этапа | Покрытие | Коммит |
|---|---|---:|---:|---|
| 0. Baseline и инструмент покрытия | ✅ выполнен | 1280 | 79% | `consolidation-0` |
| 1. Архитектурные guards | ✅ выполнен | 894 | 79% | `consolidation-1` |
| 2. Матрицы прав в api | ⬜ | ~830 | | |
| 3. Сервисный слой unit | ⬜ | ~500 | | |
| 4. knowledge, mcp, clients unit | ⬜ | ~430 | | |
| 5. Integration на PostgreSQL | ⬜ | ~370 | | |
| 6. Characterization и финал | ⬜ | ≤320 | | |

Baseline: **1280 passed**, покрытие `src` **79%** (11257 строк, 2337 не
покрыты), полный прогон **60 с**, `ruff All checks passed`.

## 1. Зачем это делается

Набор рос вместе с рефакторингом слоёв и накопил три вида избыточности.

**Параметризация по файлам.** 51 архитектурная функция разворачивается в
427 тестовых идентификаторов: каждое правило прогоняется отдельным тестом
на каждый файл пакета. Добавление файла в `src/services` механически
добавляет два десятка «тестов», хотя проверяемых правил не прибавилось.
Отчёт при этом хуже: падает первый попавшийся файл, остальные нарушения
не видны до следующего прогона.

**Один случай — один тест.** Правило «чужой или отсутствующий объект
отвечает одинаковым 404» проверяется отдельным тестом на каждый ресурс,
каждый сервис и каждый маршрут. Это одно правило, и оно должно
проверяться один раз по всему списку ресурсов, показывая сразу все
исключения.

**Дублирование по слоям.** Одно и то же поведение закреплено юнит-тестом
с моками, api-тестом и characterization-тестом. Юнит-тест с моками при
этом чаще фиксирует хореографию вызовов, а не результат: он падает при
безобидном рефакторинге и молчит при настоящей ошибке.

## 2. Принципы отбора

Тест остаётся, если верно хотя бы одно:

- он закрепляет **контракт**: URL, метод, статус, форму ответа, формат
  MCP-инструмента, правило прав, границу транзакции;
- он покрывает **нетривиальный алгоритм**: расчёт позиций и уплотнение,
  выделение цикла в ИСР, чанкинг, backoff и повторные попытки, перенос
  дат, дедупликация очереди;
- он ловит **регрессию, которую больше никто не поймает**: архитектурный
  guard, поведение на реальном PostgreSQL, компенсация внешнего файла.

Тест сворачивается или удаляется, если верно хотя бы одно:

- он проверяет, что сервис вызвал репозиторий с такими-то аргументами,
  при том что результат уже закреплён api- или integration-тестом;
- он отличается от соседнего теста одним значением параметра — такие
  случаи собираются в один табличный тест со списком всех нарушений;
- он повторяет уже закреплённое на другом слое, не добавляя ни одного
  нового утверждения о поведении.

**Запрещено** снижать покрытие ниже 78% и удалять последний тест любого
пункта Definition of Done из плана рефакторинга. Каждое удаление
обосновывается в коммите: где это поведение проверяется теперь.

## 3. Форма, к которой приводится набор

Правило вместо перечисления: один тест разбирает всё дерево и
утверждает пустоту списка нарушений.

```python
def test_service_never_reads_global_settings() -> None:
    offenders = [
        f"{path.relative_to(SRC.parent)}:{line}"
        for path in iter_python_files("services")
        for line in calls_named(ast.parse(path.read_text("utf-8")), "get_settings")
    ]
    assert not offenders, "get_settings() в сервисах: " + ", ".join(offenders)
```

Такой тест один, а сообщение содержит все нарушения сразу, а не первое.

Сценарий вместо цепочки микротестов: там, где пять тестов по очереди
дёргают один и тот же use case и проверяют по одному полю, остаётся один
тест, проходящий сценарий целиком и утверждающий все его следствия.

## 4. Этапы

### Этап 0. Baseline и инструмент покрытия

Зафиксировать 1280 passed, 79% покрытия и время прогона; добавить
`pytest-cov` в `requirements-dev.txt` как измерительный инструмент.
Покрытие — единственный объективный ограничитель сокращения.

### Этап 1. Архитектурные guards: правило вместо файла

`tests/architecture/**` — 427 идентификаторов на 51 функцию.

Каждое правило переводится на обход всего дерева со сбором полного
списка нарушений. Правила, отличающиеся только парой «пакет → запрещённый
импорт», собираются в один табличный тест с таблицей правил.

Ожидаемо: 427 → ~45, содержание проверок не меняется.

### Этап 2. Матрицы прав в api

`tests/api/test_write_scope_enforcement.py` (46) и
`tests/api/endpoints/test_access_control.py` (37) параметризованы по
маршрутам. Классификация маршрутов уже закреплена
`tests/architecture/test_route_dependency_graph.py` и
`tests/characterization/test_route_inventory.py`.

Оставить сканирующие тесты правил (READ-токен не проходит ни одну
мутацию; защищённый маршрут без сессии отвечает 401) плюс несколько
живых end-to-end проверок на представителях классов.

Ожидаемо: 182 → ~110.

### Этап 3. Сервисный слой unit

`tests/unit/services/**` — 221 функция, самый крупный пласт.

- однотипные «чужой объект → 404» по всем сервисам собрать в один
  табличный тест;
- тесты, утверждающие только факт вызова репозитория, удалить, если
  результат закреплён api- или integration-тестом;
- микротесты одного use case свести в сценарные.

Ожидаемо: ~245 → ~90.

### Этап 4. knowledge, mcp_server, clients unit

`tests/unit/knowledge/**` (68), `tests/unit/mcp_server/**` (70),
`tests/unit/clients/**` (31). Алгоритмические тесты (чанкинг, backoff,
worst-case бюджета, деление пачки заданий) сохраняются полностью;
презентеры MCP сворачиваются в один тест формата на инструмент.

Ожидаемо: ~170 → ~75.

### Этап 5. Integration на PostgreSQL

`tests/integration/**` (108). Ценность этих тестов максимальна: они
единственные проверяют реальные транзакции, server defaults и каскады.
Сокращение только за счёт однотипных проверок значений по умолчанию и
повторов одного контракта на разных репозиториях.

Ожидаемо: 108 → ~50.

### Этап 6. Characterization и финал

`tests/characterization/**` (59) — снимок публичного контракта, основная
защита от молчаливого изменения API. Сокращается минимально, только
сворачиванием параметризации.

Финал: полный прогон, сверка покрытия, запись принципов отбора в
`backend/tests/README.md`, коммит.

Ожидаемо: 59 → ~35; итог набора — **≤320 тестов**.

## 5. Команды проверки

Из каталога `backend` в PowerShell:

```powershell
.\venv\Scripts\ruff.exe check src tests
.\venv\Scripts\python.exe -m pytest -q --cov=src --cov-report=term
```

Покрытие сверяется с baseline после каждого этапа.

## 6. Definition of Done

- [ ] Тестов не больше 320, прогон не медленнее прежнего.
- [ ] Покрытие `src` не ниже 78%.
- [ ] Каждый пункт Definition of Done плана рефакторинга по-прежнему
  закреплён хотя бы одним тестом.
- [ ] Ни один архитектурный guard не потерян: список правил до и после
  совпадает.
- [ ] Сообщение упавшего guard содержит все нарушения, а не первое.
- [ ] Публичные HTTP и MCP contracts закреплены прежними
  characterization-тестами.
- [ ] Принципы отбора записаны в `backend/tests/README.md`.
- [ ] `ruff` чист, полный `pytest` зелёный, integration реально на
  PostgreSQL.
