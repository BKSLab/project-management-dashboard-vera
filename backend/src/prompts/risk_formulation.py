"""Отдельные системные prompts для полей риска."""

from src.prompts.base import build_system_prompt

_COMMON = """Работай только с данными риска из JSON. Сохраняй смысл пользовательских формулировок и не выдумывай факты, даты, людей или системы. Верни JSON вида {\"text\": \"...\"}. Текст должен быть конкретным, проверяемым и пригодным для Markdown. Пользователь проверит и примет результат вручную."""

RISK_DESCRIPTION_PROMPT = build_system_prompt(
    _COMMON + " Сформулируй описание риска: возможное событие, причины и последствия для проекта."
)
RISK_MITIGATION_PROMPT = build_system_prompt(
    _COMMON
    + " Сформулируй превентивный план митигации: действия до реализации события для снижения вероятности или влияния. Учитывай выбранную стратегию."
)
RISK_RESPONSE_PROMPT = build_system_prompt(
    _COMMON
    + " Сформулируй план реагирования: последовательность действий, если риск реализуется или потребует реакции. Учитывай статус, вероятность, влияние и стратегию."
)
